
const INITIAL_STATE = {
  costoMase: 0
};

function settingsReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}


export default settingsReducer;
